# Install

```bash
# Installing XCode...
xcode-select --install

# Installing HomeBrew...
curl -so /tmp/brew_install.sh https://raw.githubusercontent.com/Homebrew/install/master/install.sh
bash /tmp/brew_install.sh
rm -f /tmp/brew_install.sh

make all
```
